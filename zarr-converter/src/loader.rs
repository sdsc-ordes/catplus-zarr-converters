

pub fn load_metadata(input_content: &str)-> Result<String> {
    // Here we load the RDF file
}

pub fn load_data(input_content: &str){
    // Here we load the data cubes
    // we might need a loader per data type
}